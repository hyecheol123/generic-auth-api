/**
 * Type definition for the Name of DB Table that is needed for each test
 *
 * @author Hyecheol (Jerry) Jang <hyecheol123@gmail.com>
 */

/**
 * Enum Indicating the Name of DB Tables
 */
enum DBTable {
  USER = 'user',
  SESSION = 'session',
}

export default DBTable;
